[project]
name = "knowledge-base-roboto"
version = "0.1.0"
description = "Roboto Python SDK source code and client library"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "chromadb",
    "pyyaml",
    "tree-sitter",
    "tree-sitter-typescript",
    "tree-sitter-javascript",
]
